sudo: required

language: ruby

services:
  - docker

before_install:
- docker build -t gibbon .
- docker run -it -d -p 8080:80 kerrongordon/gibbon
- docker images
- docker ps -a
- docker push kerrongordon/gibbon

